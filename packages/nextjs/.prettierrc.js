module.exports = {
  arrowParens: "avoid",
  printWidth: 120,
  tabWidth: 2,
  trailingComma: "all",
  // Note: Import sorting plugin temporarily disabled due to duplicate comment issue
  // Can be re-enabled once the issue is resolved
  // importOrder: ["^react$", "^next/(.*)$", "<THIRD_PARTY_MODULES>", "^@heroicons/(.*)$", "^~~/(.*)$"],
  // importOrderSortSpecifiers: true,
  // plugins: [require.resolve("@trivago/prettier-plugin-sort-imports")],
};
